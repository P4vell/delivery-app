export type Restaurant = {
  name: string;
  type: string;
  image: string;
  rating: number;
  ratingsCount: number;
};

export type CartItem = {
  name: string;
  image: string;
  quantity: number;
  price: number;
};
